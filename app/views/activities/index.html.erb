<div class="index-page-content">
  <div class="container">
    <h3>Como podemos lhe ajudar hoje?</h3>
    <p>Escolhe uma categoria</p>
    <div class="d-flex justify-content-between my-4">
      <% Activity::ACTIVITY_CATEGORIES.each do |category| %>
        <%= link_to("#{category}", activities_path(category: "#{category}"), class: "btn-category") %>
      <% end %>
    </div>
    <div class="d-flex justify-content-between flex-wrap">
      <% @activities.each do |activity| %>
        <%= link_to(activity_path(activity)) do %>
          <%= render('shared/card-activity', act_photo: activity.photos.present? ? activity.photos[0].key : false, title: activity.title, category: activity.category, price: activity.price, act_user_photo:  activity.user.photo.present? ? activity.user.photo.key : false) %>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
